---
"@shopify/shop-cli": minor
---

Identify Shop CLI traffic using a standard `User-Agent` header (`shop-cli/<version>`) on every outbound request, instead of a custom `X-Shopify-Agent-Source` header.
